package lambdaexpressions;

// This is a functional interface. Functional interface is an interface that only has one method in it, e.g. Comparable, Runnable interfaces.

public interface Executable {

    int execute(int a, int b);
}
